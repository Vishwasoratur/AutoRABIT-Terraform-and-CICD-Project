version: 0.2
phases:
  pre_build:
    commands:
      - echo "Logging in to Amazon ECR..."
      - ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)
      - REGION=$(aws configure get region)
      - ECR_REPO_URI="$ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com/devops-project-repo"
      - $(aws ecr get-login-password --region $REGION | docker login --username AWS --password-stdin $ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com)
      - echo "Authenticating with ECR successful."
      - echo "Updating terraform state with S3 backend configuration..."
      - cd terraform
      # CORRECTED: The bucket name has been updated to reflect the new region.
      - terraform init -backend-config="bucket=vishwa-devops-project-terraform-state-2025-us-west-2" -backend-config="key=devops-project/terraform.tfstate" -backend-config="region=$REGION" -backend-config="dynamodb_table=vishwa-devops-project-terraform-locks"
      - echo "Terraform initialized."

  build:
    commands:
      - echo "Building the Docker image..."
      - docker build -t $ECR_REPO_URI:latest ..
      - docker tag $ECR_REPO_URI:latest $ECR_REPO_URI:$CODEBUILD_RESOLVED_SOURCE_VERSION
      - echo "Image built and tagged."

  post_build:
    commands:
      - echo "Pushing Docker images to ECR..."
      - docker push $ECR_REPO_URI:latest
      - docker push $ECR_REPO_URI:$CODEBUILD_RESOLVED_SOURCE_VERSION
      - echo "Docker images pushed to ECR."
      - echo "Running Terraform to update infrastructure (if needed)..."
      # CORRECTED: The github_connection_arn must be the new one for us-west-2.
      # You need to replace the placeholder below with the actual ARN from the AWS Console.
      - terraform apply -auto-approve -var="github_owner=Vishwasoratur" -var="github_repo_name=AutoRABIT-Terraform-and-CICD-Project" -var="github_connection_arn=arn:aws:codeconnections:us-west-2:058264310461:connection/a46e6721-0e14-490e-9ba8-e2e7d5a1f478"
      - echo "Terraform applied."

artifacts:
  files:
    - imagedefinitions.json




